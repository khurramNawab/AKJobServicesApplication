{
  "name": "job-portal-unified",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && npm install --prefix web --include=dev --legacy-peer-deps && npm run build --prefix web",
    "postinstall": "npm run build"
  }
}