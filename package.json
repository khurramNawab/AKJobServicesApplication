{
  "name": "ak-job-services-deployment",
  "version": "1.0.0",
  "description": "Root package for Hostinger Unified Deployment",
  "type": "module",
  "scripts": {
    "postinstall": "cd backend && npm install --no-audit --no-fund",
    "build": "echo 'Frontend pre-built. Skipping build.'",
    "start": "node server.js"
  }
}