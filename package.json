{
  "name": "ak-job-services-deployment",
  "version": "1.0.0",
  "description": "Root package for Hostinger Unified Deployment",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../web && npm install",
    "build": "cd web && npm run build",
    "start": "cd backend && node server.js"
  }
}